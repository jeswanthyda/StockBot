from utils import MDB,TradeStrategy

if __name__ = "__main":
    while True:
        #Step1 - update hot stocks - should be one line call (check for 9 am and tackling inventory has to happen in some class method)
        #Step2 - get all stock symbols - one liner
        # Inside for loop
            #Step3 - generate action signal for stock - one liner
            #step4 - take corresponding action - one liner
